
repositories{
    jcenter()
    flatDir{
        dirs 'libs'
        // dirs 'src/main/libs'
    }
}

dependencies {
    compile(name:'Barcode-release', ext:'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}
