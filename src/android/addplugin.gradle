
repositories{
    jcenter()
    flatDir{
        dirs 'libs'
        // dirs 'src/main/libs'
    }
}

dependencies {
    compile(name:'Barcode-release1', ext:'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}
