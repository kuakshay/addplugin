
android {
    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 22
    }
    compile fileTree(dir: 'libs', include: '*.jar')
compile 'androidx.appcompat.app.AppCompatActivity'

   packagingOptions {
       exclude 'META-INF/NOTICE'
       exclude 'META-INF/LICENSE'
   }
}